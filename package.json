{
  "name": "playwright-test",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end test for a critical ConsignO Cloud workflow",
  "scripts": {
    "auth": "playwright test auth.setup.spec.ts --headed",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "dotenv": "^17.3.1"
  }
}
